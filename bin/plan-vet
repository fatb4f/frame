#!/usr/bin/env sh
set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd -P)
repo_dir=$(CDPATH= cd -- "$script_dir/.." && pwd -P)

command -v cue >/dev/null 2>&1 || {
  printf 'plan-vet: cue not found\n' >&2
  exit 2
}

cd "$repo_dir"
exec cue vet ./cue "$@"

